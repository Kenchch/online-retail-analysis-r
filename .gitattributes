# Let git normalise text in the repository and check it out in the platform
# convention, so a clone on Windows and a clone on Linux hold the same bytes.
* text=auto

# Binary assets -- never diff these and never touch their line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary

# ggplot2 output committed as the report's figures; text, but never merged.
*.svg -merge
